# 機材管理システム ダッシュボード仕様

## 前提

- ダッシュボードは管理者・登録者 / 運営者の両方が閲覧可能とする。
- 修理状況、故障傾向、費用状況を素早く把握できることを目的とする。
- 表示データは検索条件で絞り込めるようにする。
- ガントチャートは月単位で表示し、PDF出力に対応する。

## 表示内容

| 表示項目 | 内容 |
|---|---|
| 修理ステータス別件数 | 未対応、確認中、修理依頼済み、修理中、完了、キャンセルなどの件数 |
| 場所ごとの故障件数 | 事故・故障場所別の件数 |
| 月別故障件数 | 発生日時ベースの月別件数 |
| 場所ごとの修理費用 | 事故・故障場所別の修理費用合計 |
| 機材別修理費用 | 機材単位の累計修理費用 |
| 未完了修理一覧 | 完了扱いでない修理情報 |
| 完了希望日が近い修理一覧 | 完了希望日が近い未完了修理 |
| ガントチャート | 月単位の修理予定・進捗 |

## 共通絞り込み条件

| 項目 | 入力形式 | 備考 |
|---|---|---|
| 対象期間 From | 日付 | 発生日時または依頼日ベース |
| 対象期間 To | 日付 |  |
| 場所 | 場所マスター選択 |  |
| 修理ステータス | 修理ステータスマスター選択 |  |
| 担当者 | ユーザーまたは担当者名 |  |
| 修理依頼先 | マスターまたは自由入力名 |  |

## 修理ステータス別件数

### 集計条件

- `wp_equipment_repairs.status_id` を基準に集計する。
- 修理ステータスマスターの有効データを表示対象にする。

### 表示形式

- 件数カード
- 棒グラフまたは円グラフ

## 場所ごとの故障件数

### 集計条件

- `wp_equipment_repairs.trouble_location_id` を基準に集計する。
- 発生日時で期間絞り込みを行う。

### 表示形式

- 場所別ランキング
- 棒グラフ

## 月別故障件数

### 集計条件

- `wp_equipment_repairs.occurred_at` を年月単位で集計する。

### 表示形式

- 折れ線グラフ
- 棒グラフ

## 場所ごとの修理費用

### 集計条件

- `wp_equipment_repairs.trouble_location_id` を基準に、`repair_cost` を合計する。
- 修理費用が未入力のデータは0円として扱う。

### 表示形式

- 場所別費用ランキング
- 棒グラフ

## 機材別修理費用

### 集計条件

- `wp_equipment_repairs.equipment_id` を基準に、`repair_cost` を合計する。

### 表示形式

- 上位件数ランキング
- 機器名、設置場所、型番、機器コード、累計修理費用を表示する。

## 未完了修理一覧

### 抽出条件

- 修理ステータスマスターの `is_closed = 0` のデータを対象にする。

### 表示項目

| 項目 | 内容 |
|---|---|
| 修理ID |  |
| ステータス |  |
| 発生日時 |  |
| 機器名 |  |
| 場所 |  |
| 担当者 |  |
| 完了希望日 |  |
| 修理依頼先 |  |

## 完了希望日が近い修理一覧

### 抽出条件

- 修理ステータスマスターの `is_closed = 0` のデータを対象にする。
- 完了希望日が当日から7日以内のデータを表示する。
- 完了希望日を過ぎているデータも表示する。

### 表示項目

- 修理ID
- ステータス
- 機器名
- 場所
- 担当者
- 完了希望日
- 期限超過日数

## ガントチャート

### 表示単位

- 月単位で表示する。
- 検索条件により任意の年月範囲を指定できる。

### 表示対象日付

| 日付 | 用途 |
|---|---|
| 依頼日 | ガント開始日の候補 |
| 完了希望日 | 予定終了日 |
| 完了日 | 実績終了日 |

### 表示項目

| 項目 | 内容 |
|---|---|
| 機器名 | 対象機材 |
| 設置場所 | 対象機材または事故・故障場所 |
| ステータス | 修理ステータス |
| 修理依頼先 | マスター名または自由入力名 |
| 担当者 |  |
| 依頼日 |  |
| 完了希望日 |  |
| 完了日 |  |

## ガントチャートPDF

### 出力条件

- 画面上の検索条件を反映する。
- 月単位で出力する。
- PDFには出力日時と検索条件を表示する。

### 出力項目

- 機器名
- 設置場所
- ステータス
- 修理依頼先
- 担当者
- 依頼日
- 完了希望日
- 完了日
- 月単位のガント表示

## 注意事項

- 5,000件規模の機材と修理履歴を想定し、集計クエリには適切なインデックスを利用する。
- ダッシュボード初期表示では、対象期間を直近12か月などに限定し、表示負荷を抑える。
- グラフ描画はJavaScriptライブラリを利用する。
- PDF出力は日本語フォント対応が必要になる。
- ガントチャート画面は `admin/class-gantt-page.php` と `assets/js/gantt.js` で制御する。
- PDF生成は `includes/class-pdf.php` でmPDFを呼び出し、`templates/pdf/gantt-pdf.php` をPDF用HTMLテンプレートとして利用する。
